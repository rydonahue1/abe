import config from "../config.json"

export const token = config.token;
export const firebaseAdmin = config.firebase_admin;
export const firebaseBucket = config.firebase_bucket;
export const youtubeKey = config.youtubeKey